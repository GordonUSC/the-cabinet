(()=>{'use strict';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
window.ggpRecordEvidence=(r,compact=false)=>{const es=r.publicRecords||[];if(!es.length)return '';if(compact)return `<p class="record-operator small"><b>Operated / supported by:</b> ${es.map(e=>esc(e.entity)).join(' · ')}<br><span>${es.map(e=>esc(e.kind)).join(' · ')}</span></p>`;return `<details class="record-evidence"><summary>Who operates this? Public records + limitations</summary><p class="small">Identity and source lookup · September 7, 2026. Records are not a completed financial audit or a verified donation route.</p>${es.map(e=>`<section><h4>${esc(e.entity)}</h4><p><span class="tag">${esc(e.kind)}</span>${e.ein?` <b>EIN ${esc(e.ein)}</b>`:''}</p><p>${esc(e.note)}</p>${e.url?`<a target="_blank" rel="noopener" href="${esc(e.url)}">Open ${/filing|financial|records/i.test(e.kind)?'public records':'identity source'} ↗</a>`:'<p>Legal entity still needs identification.</p>'}${e.relationshipSource?` · <a target="_blank" rel="noopener" href="${esc(e.relationshipSource)}">Verify program relationship ↗</a>`:''}</section>`).join('')}<p class="small">Organization-wide revenue, assets and grants must not be treated as this program’s budget. An absent filing here means unresolved coverage, not proof of noncompliance.</p></details>`;};
})();
