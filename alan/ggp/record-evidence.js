(()=>{'use strict';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
window.ggpRecordEvidence=(r,compact=false)=>{const es=r.publicRecords||[];if(compact)return es.length?`<p class="record-operator small"><b>Organization reference:</b> ${es.map(e=>esc(e.entity)).join(' · ')}<br><span>${es.map(e=>`<span title="${esc(e.note)}">${esc(e.kind)}</span>`).join(' · ')}</span></p>`:'';return `<details class="record-evidence"><summary>Sources, organization and evidence limits</summary><p class="small">Program information reviewed ${esc(r.checked)}. Identity sources checked ${esc(r.identityChecked||'2026-09-07')}. These are separate checks; neither proves that applications or donations are currently available.</p>${(r.sources||[]).length?`<ul>${r.sources.map(([label,url])=>`<li><a href="${esc(url)}" target="_blank" rel="noopener">${esc(label)} ↗</a></li>`).join('')}</ul>`:''}${es.map(e=>`<section><h4>${esc(e.entity)}</h4><p><span class="tag" title="${esc(e.note)}">${esc(e.kind)}</span>${e.ein?` <b>EIN ${esc(e.ein)}</b>`:''}</p><p>${esc(e.note)}</p>${e.url?`<a target="_blank" rel="noopener" href="${esc(e.url)}">Open identity or public-record source ↗</a>`:'<p>Legal entity still needs identification.</p>'}${e.relationshipSource?` · <a target="_blank" rel="noopener" href="${esc(e.relationshipSource)}">Verify program relationship ↗</a>`:''}</section>`).join('')}<p class="small">Organization-wide revenue, assets and grants must not be treated as this program’s budget. A missing filing here means unresolved coverage, not proof of noncompliance.</p></details>`;};
})();
