<!doctype html>
<html lang="en"><head>
<!-- STYLE: Run of show, broadcast script | WHY: this is not a document to read, it is a thing three people perform standing up against a visible timer, so it is set like a live script: a hard timecode rail down the left that you can find with your thumb mid-sentence, the spoken words in large type because you glance and look back up, and stage directions in a different voice entirely so nobody reads a cue out loud by accident. | ALT: the exhibition catalogue from the mood board page, rejected because a catalogue is for a reader going at their own pace and this has a clock on it; also because I shipped that page tonight. -->
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<meta name="robots" content="noindex">
<title>Mood board presentation: two outstanding versions</title>
<style>
*{box-sizing:border-box;margin:0;padding:0}
:root{--paper:#fcfbf8;--ink:#15181d;--mut:#6d7480;--line:#e3e0d8;--red:#b23b28;--blue:#2e5f8a}
html{-webkit-text-size-adjust:100%}
body{background:var(--paper);color:var(--ink);
  font:17px/1.6 "Iowan Old Style",Georgia,"Times New Roman",serif;padding:0 0 70px}
.wrap{max-width:860px;margin:0 auto;padding:0 22px}
.ui{font-family:"Helvetica Neue",Helvetica,Arial,sans-serif}
.tc{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace}
a{color:var(--blue)}

header{padding:42px 0 22px;border-bottom:3px double var(--ink)}
.kick{font:700 11px/1 "Helvetica Neue",Helvetica,Arial,sans-serif;letter-spacing:.2em;
  text-transform:uppercase;color:var(--red)}
h1{font-size:40px;line-height:1.05;letter-spacing:-.022em;margin:14px 0 0;font-weight:600}
h1 em{font-style:italic;color:var(--mut);display:block;font-size:20px;font-weight:400;margin-top:10px;
  letter-spacing:0}
.rules{display:grid;grid-template-columns:repeat(auto-fit,minmax(168px,1fr));gap:14px;margin-top:22px}
.rules div{border-top:2px solid var(--ink);padding-top:9px}
.rules b{display:block;font:700 11px/1.3 "Helvetica Neue",Helvetica,Arial,sans-serif;
  letter-spacing:.12em;text-transform:uppercase}
.rules span{display:block;font-size:14.5px;color:var(--mut);margin-top:5px;line-height:1.45}

.lede{margin:26px 0 0;font-size:18px;max-width:66ch}
.lede + .lede{margin-top:12px}
.flag{margin-top:20px;padding:15px 18px;border-left:3px solid var(--red);background:#f6f1ec;font-size:15.5px}

h2.ver{font-size:30px;letter-spacing:-.02em;margin:46px 0 0;padding-top:26px;border-top:1px solid var(--line)}
h2.ver small{display:block;font:700 11px/1 "Helvetica Neue",Helvetica,Arial,sans-serif;
  letter-spacing:.2em;text-transform:uppercase;color:var(--red);margin-bottom:11px}
.shape{font-size:16px;color:var(--mut);margin-top:10px;max-width:64ch}
.runs{font:600 13px/1.5 ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;color:var(--ink);
  margin-top:12px;padding:9px 13px;background:#f3efe6;border-left:3px solid var(--red);max-width:64ch}
.runs b{color:var(--red)}

.credits{margin:18px 0 6px;border:1px solid var(--line);background:#fff}
.credits h3{font:700 10.5px/1 "Helvetica Neue",Helvetica,Arial,sans-serif;letter-spacing:.16em;
  text-transform:uppercase;color:var(--mut);padding:12px 16px 0}
.credits ul{list-style:none;padding:10px 16px 14px;display:grid;
  grid-template-columns:repeat(auto-fit,minmax(215px,1fr));gap:12px}
.credits li{font-size:14.5px;line-height:1.5}
.credits .who{font:700 13px/1.3 "Helvetica Neue",Helvetica,Arial,sans-serif;letter-spacing:.04em;
  display:block;color:var(--ink)}
.credits .did{color:var(--mut)}

.row{display:grid;grid-template-columns:96px 1fr;gap:0 20px;border-top:1px solid var(--line);
  padding:20px 0}
.rail{border-right:1px solid var(--line);padding-right:16px}
.rail .t{font:700 14px/1.3 ui-monospace,SFMono-Regular,Menlo,monospace;color:var(--red)}
.rail .who{font:700 11px/1.35 "Helvetica Neue",Helvetica,Arial,sans-serif;letter-spacing:.1em;
  text-transform:uppercase;margin-top:7px}
.rail .len{font:400 11px/1.3 ui-monospace,SFMono-Regular,Menlo,monospace;color:var(--mut);margin-top:4px}
.say{font-size:18.5px;line-height:1.58}
.say p + p{margin-top:13px}
.cue{font:italic 14.5px/1.5 "Iowan Old Style",Georgia,serif;color:var(--blue);
  margin:12px 0;padding-left:13px;border-left:2px solid #cddbe6}
.cue b{font-style:normal;font-weight:700;font-family:"Helvetica Neue",Helvetica,Arial,sans-serif;
  font-size:10.5px;letter-spacing:.12em;text-transform:uppercase;color:var(--blue);
  display:block;margin-bottom:3px}
.term{font-weight:700;border-bottom:2px solid #e8d9a8;padding-bottom:1px}

.after{margin-top:30px;padding:20px;border:1px solid var(--line);background:#fff}
.after h3{font-size:18px;margin-bottom:10px}
.after p{font-size:15.5px;color:var(--mut)}
.after p + p{margin-top:9px}
.after b{color:var(--ink)}

footer{margin-top:48px;padding-top:18px;border-top:1px solid var(--line);
  font:13px/1.65 "Helvetica Neue",Helvetica,Arial,sans-serif;color:var(--mut)}

@media (max-width:640px){
  h1{font-size:29px}
  h2.ver{font-size:24px}
  .row{grid-template-columns:1fr;gap:10px}
  .rail{border-right:0;border-bottom:1px solid var(--line);padding:0 0 9px;
    display:flex;align-items:baseline;gap:12px}
  .rail .who,.rail .len{margin-top:0}
  .say{font-size:17px}
}
</style></head>
<body>
<div class="wrap">

